import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.tellevoKYA.app',
  appName: ' TellevoKYA',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
